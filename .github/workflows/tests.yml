name: tests

on: workflow_dispatch
# on:
#   push:
#     branches:
#       - main
#   pull_request:
#     branchs:
#       - 'feature/**'
#       - 'fix/**'
#       - 'refactor/**'


jobs:
  test:
    # runs-on: arc-runner-set
    runs-on: self-hosted
    name: Testes
    steps:
      - name: checkout PR merge commit
        uses: actions/checkout@v3

      - name: Rodar um echo
        run: echo 'Olá mundo!'

      - name: Metadatas
        run: |
          # cat /etc/os*release
          # cat /etc/group
          uname -a
          id
          # sudo id
          cat $HOME/logs.txt || true
          echo FOO-BAR > $HOME/logs.txt

      - name: Testes sobre docker
        run: |
          # env | sort
          # touch /dev/kvm
          # which docker
          docker --version
          # sudo docker info
          docker info
          docker images

      - name: Executa instalação via nix para teste mínimo
        run: |
           nix profile install nixpkgs#hello nixpkgs#python3
           hello
           python3 -c 'import this'
